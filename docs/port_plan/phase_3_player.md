# Phase 3: Player Entity & Interactivity

## Steps

- [ ] Assemble `Player.tscn` by composing reusable components onto a `CharacterBody2D` host.
- [ ] Add `PlayerInputComponent` for player-specific input handling.
- [ ] Implement basic collisions against `TileMapLayer` walls via `MovementComponent`.
- [ ] Recreate Elevators as `Area2D` triggers with level-transition logic.
- [ ] Recreate Doors as animated `StaticBody2D` scenes.
- [ ] Recreate Terminals/Consoles as `Area2D` interaction triggers.
